#include <bits/stdc++.h>

using namespace std;

int main() {
    int ev1, ev2, at, d;
    double prob;

    while (1) {
        cin >> ev1 >> ev2 >> at >> d;
        if (ev1 == 0 && ev2 == 0 && at == 0 && d == 0) {
            break;
        }

        cout << fixed << setprecision(2) << endl;
    }

    return 0;
}